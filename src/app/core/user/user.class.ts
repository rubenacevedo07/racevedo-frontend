class User {
    id: string;
    name: string;
    email: string;
    avatar?: string;
    status?: string;

    constructor(id: number, name: string, email: string, avatar: string, status: string) {
            this.id = "1";
            this.name = name;
            this.email = name;
            this.avatar = avatar;
            this.status = status;
    }

}